import React, {useState} from 'react';
import {
  Button,
} from '@patternfly/react-core';

const Views = () => {

  return (
    <React.Fragment>
      <div>This is the views page.</div>
    </React.Fragment>
  );
}

export {Views};
