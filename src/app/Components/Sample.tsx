import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Sample = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Sample is Empty</Title>
  </PageSection>
);

export { Sample };
